# =============================================================================
# SSH Config - DevOps Coursework
# Auto-generated by Terraform
# DO NOT EDIT MANUALLY - Regenerated on terraform apply
# =============================================================================

# -----------------------------------------------------------------------------
# Global Settings (for all hosts in this project)
# -----------------------------------------------------------------------------
Host *
    User ubuntu
    IdentityFile ~/.ssh/terr
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    LogLevel ERROR
    ServerAliveInterval 60
    ServerAliveCountMax 3

# -----------------------------------------------------------------------------
# Bastion Host (direct connection)
# -----------------------------------------------------------------------------
Host bastion
    HostName ${bastion_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr

# -----------------------------------------------------------------------------
# Web Servers (through Bastion)
# -----------------------------------------------------------------------------
Host web-a-1
    HostName ${web_a_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr
    ProxyJump bastion

Host web-b-1
    HostName ${web_b_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr
    ProxyJump bastion

# -----------------------------------------------------------------------------
# Elasticsearch (through Bastion)
# -----------------------------------------------------------------------------
Host elasticsearch
    HostName ${elasticsearch_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr
    ProxyJump bastion

# -----------------------------------------------------------------------------
# Kibana (through Bastion)
# -----------------------------------------------------------------------------
Host kibana
    HostName ${kibana_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr
    ProxyJump bastion

# -----------------------------------------------------------------------------
# Prometheus (through Bastion)
# -----------------------------------------------------------------------------
Host prometheus
    HostName ${prometheus_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr
    ProxyJump bastion

# -----------------------------------------------------------------------------
# Grafana (through Bastion)
# -----------------------------------------------------------------------------
Host grafana
    HostName ${grafana_ip}
    User ubuntu
    IdentityFile ~/.ssh/terr
    ProxyJump bastion